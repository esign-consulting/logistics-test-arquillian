<arquillian xmlns="http://jboss.org/schema/arquillian" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="http://jboss.org/schema/arquillian http://jboss.org/schema/arquillian/arquillian_1_0.xsd">

    <container qualifier="widlfly-remote" default="true">
        <configuration>
            <property name="managementAddress">${host:localhost}</property>
            <property name="managementPort">${mgmtPort:9990}</property>
            <property name="username">${username:admin}</property>
            <property name="password">${password:Admin#70365}</property>
            <property name="host">${host:localhost}</property>
            <property name="port">${port:8080}</property>
        </configuration>
    </container>
    
</arquillian>